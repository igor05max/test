создал файл
clean_cache.sh

#!/bin/bash

sudo apt-get clean
sudo apt-get autoclean
echo "OK"


консоль: 
sudo chmod +x /usr/local/bin/clean_cache.sh
crontab -e

в конец написал 
0 16 1 * * /usr/local/bin/clean_cache.sh

